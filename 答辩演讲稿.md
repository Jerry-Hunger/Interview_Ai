# IntelliHire 答辩功能展示演讲稿

> 根据 PPT 实际截图内容、论文第四章系统实现、以及项目源码综合编写。

---

## 第 1 页 — 封面页

各位老师好，我是计算机学院计算机科学与技术专业的周勇，我的毕业设计题目是《基于 AI 驱动的面试与招聘平台的设计与实现》，指导老师是叶斌老师。接下来我将为各位老师演示系统各模块的功能实现。

---

## 第 2 页 — 目录页

本次功能展示分为三个部分：首先是网站首页展示，然后是学生端的核心功能实现，最后是企业端的功能实现。

---

## 第 3 页 — 网站首页展示

这是 IntelliHire 平台的首页，支持明暗两种主题。页面顶部是导航栏和品牌标语"掌握您的下一次面试"。中部展示平台核心数据：10000+模拟面试、500+合作企业、95%满意度、3倍通过率提升。下方是四大核心功能卡片：AI模拟面试、企业招聘管理、实时反馈和简历智能分析。底部是用户评价区。

技术实现上，首页由 `Index.tsx` 组件实现，页面分为四个模块：Hero 品牌区使用渐变背景和标语引导用户注册；统计数据区以 4 列网格展示平台指标；功能特性区渲染 4 张功能卡片；用户评价区展示含星级评分的静态评价卡片。导航栏由 `Navigation.tsx` 实现，它从 localStorage 读取 token 和 role 字段，根据用户登录状态和角色动态切换导航项：未登录显示首页、登录、注册；学生角色显示仪表盘、模拟面试、浏览职位等；企业角色显示仪表盘、发布职位、职位列表等。暗色主题通过 Tailwind CSS 的 `.dark` class 切换，由 ThemeToggle 组件控制。

---

## 第 4 页 — 用户认证模块（过渡页）

接下来展示用户认证模块。

---

## 第 5 页 — 用户注册与登录界面展示

左侧是注册界面，右侧是登录界面。注册时选择学生或企业角色，填写姓名、邮箱和密码完成注册。登录支持邮箱密码和 GitHub OAuth 两种方式。

技术实现上，后端 `authController.js` 的 `register` 函数接收注册请求后，首先通过 `User.findOne` 检查邮箱是否已注册，然后调用 `bcrypt.hash` 对密码做加盐哈希处理（saltRounds=10）。接着通过 `User.create` 创建认证文档，再根据角色调用 `getProfileModel` 获取对应的 Student 或 Company 模型，创建 Profile 文档并设置 `_id` 等于 `user._id`，两者共享同一个 `_id` 实现一对一嵌入式关联。注册成功后调用 `jwt.sign` 签发 JWT，payload 包含 id 和 role，有效期 1 天。登录函数通过 `bcrypt.compare` 比对密码哈希，验证通过后同样签发 JWT。

GitHub OAuth 流程较为复杂：用户点击后后端重定向到 GitHub 授权页，授权回调后后端用授权码向 GitHub 换取 access_token，再调用 GitHub API 获取用户信息。匹配逻辑是先按 githubId 查找用户，再按 email 查找——已有账户则关联 githubId 实现账户合并，未注册则自动创建新用户。GitHub 登录签发 30 天有效的 JWT。前端通过 axios 拦截器统一将 JWT 添加到 Authorization 请求头，后端 `authMiddleware.js` 从请求头提取 Bearer Token 并验证，将解码后的用户信息挂载到 `req.user` 供后续路由使用。

---

## 第 6 页 — 学生端功能实现（过渡页）

接下来展示学生端的核心功能。

---

## 第 7 页 — 学生端仪表盘界面展示

这是学生面试练习仪表盘。顶部四个统计卡片显示总面试数 12 次、通过 9 次、未通过 1 次、已退出 2 次，通过率 75%。下方按结果分组展示面试记录列表，点击可查看详情。

技术实现上，前端调用 `GET /interview/mine` 接口，后端通过 `Interview.find({ student: studentId }).sort({ createdAt: -1 })` 查询当前学生的所有面试记录，按创建时间降序返回。前端拿到数据后按 result 字段（success/failure/quit）分组渲染为三个折叠区域，每张卡片通过 type 字段显示"企业面试"或"练习面试"，difficulty 字段映射为不同颜色的难度标签。统计卡片通过遍历面试数组计算各状态的数量和通过率。

---

## 第 8 页 — 模拟面试配置界面展示

这是模拟面试的配置界面，左右两栏布局。左侧配置表单包含六个参数：简历上传、目标职位、难度等级、面试类型、面试轮次（1-3轮）和每轮问题数（5-8个）。右侧是面试技巧和内容预览。

配置完成后点击"开始面试"，前端调用 `POST /interview/start` 将简历文本、职位、难度等参数发送到后端。后端 `interviewController.js` 的 `startInterview` 函数根据是否为续轮选择不同的提示词：首轮使用 `startInterviewFirstRound`，它会将简历、职位、难度等信息注入提示词模板，定义 AI 面试官"艾莎"的角色设定，并约束每轮只能提一个问题；续轮使用 `startInterviewContinuationRound`，额外注入 currentRound、totalRounds 和 previousFeedback 参数。提示词构建完成后，调用 `utils/deepseek.js` 封装的 DeepSeek API（模型 deepseek-v4-flash，temperature 0.7）生成开场白和第一个问题，返回给前端展示。简历上传由 `ResumeUploader.tsx` 组件处理，文件通过 `POST /upload/resume` 上传至阿里云 OSS，后端同时创建 Resume 文档并关联到 Student。

---

## 第 9 页 — 模拟面试界面展示

这是面试对话界面，三栏布局。左侧显示面试元数据：职位、类型和当前进度。中间区域上方是 AI 面试官头像和摄像头预览，下方是回答输入框。右侧是对话历史面板，支持 Markdown 渲染。

技术实现上，对话采用 SSE 流式传输。前端使用原生 fetch 而非 axios，通过 `response.body.getReader()` 获取 ReadableStream，在循环中调用 `reader.read()` 逐块读取数据，用 `TextDecoder` 解码后通过 `setStreamingMessage` 实时更新 UI。后端 `respondToInterviewStream` 函数设置了三个关键响应头：`Content-Type: text/event-stream`、`Cache-Control: no-cache` 和 `X-Accel-Buffering: no`（兼容 Nginx 反向代理），然后通过 `for await` 循环遍历 `streamDeepSeekResponse` 异步生成器逐块写入 AI 回复。同时监听 `req` 的 `close` 事件，在客户端断开时停止推送。

在敷衍检测方面，`prompts/interview.js` 的 `respondNormal` 提示词要求 AI 判断回答质量——如果回答少于 5 个字、内容空洞或与问题无关，AI 必须以 `[REPROMPT]` 标签开头要求重新回答。前端通过正则 `/\[REPROMPT\]/i` 检测到该标签后，弹出 toast 提示学生完善回答，关键是不会递增 `currentQuestion` 计数器，让学生重新回答同一题。此外还集成了浏览器原生 Web Speech API，语音识别使用 `SpeechRecognition` 接口（continuous=true, interimResults=true），语音播报使用 `SpeechSynthesis` 接口（pitch=1.2 模拟女声），匹配"艾莎"的女性角色设定。

---

## 第 10 页 — 面试结果界面展示（一）

左侧是通过的结果页，显示绿色对勾和"恭喜通过"。右侧是未通过的结果页。分项反馈按每3对问答一组展示，包含回答概述、优点、不足和建议四个维度。

技术实现上，评估采用分块策略。后端 `concludeInterviewStream` 函数首先将 chatHistory 按 `CHUNK_SIZE=6`（即 3 对问答）分块，过滤掉末尾可能的 AI 总结条目确保问答对完整。然后对每个 chunk 调用 `concludeChunk` 提示词生成器，它要求 AI 从回答概述、优点、不足、建议四个维度评估。每个 chunk 的评估通过 `streamDeepSeekResponse` 流式返回，后端用自定义标记协议标识进度：`[CHUNK_START: N]` 开始第 N 块、`[CHUNK_END: N: base64]` 结束并携带完整反馈的 base64 编码。所有分块完成后，调用 `concludeFinal` 汇总提示词，将所有 feedbacks 一起注入，要求 AI 生成整体表现、关键亮点、待改进和综合建议，并严格输出"评估结论：通过"或"评估结论：不通过"。最后通过 `parseResultFromFeedback` 函数使用正则 `/(?:评估结论|结果)\s*[：:]\s*(不?通过)/` 自动提取结果，以 `[DONE: interviewId: result]` 标记结束。前端 `PracticeResults.tsx` 按三阶段状态机（idle→chunk→final）解析协议，实时渲染分块反馈和最终评价。DeepSeek API 调用封装了指数退避重试机制：4xx 客户端错误立即抛出不重试，5xx 服务端错误按 `1000×2^attempt` 毫秒间隔重试，最多重试 2 次。

---

## 第 11 页 — 面试结果界面展示（二）

这两张截图展示了多轮面试场景。左侧是第一轮通过后的结果，显示"您已通过！等待最终审核结果"。右侧是未通过的最终反馈页面。

多轮面试的衔接机制较为复杂。前端 `PracticeResults.tsx` 的 `canContinue` 函数按优先级依次判断：当前轮次是否小于总轮数、本轮结果是否为 success、所有前序轮次是否都是 success。企业面试（type: "company"）强制不显示续轮按钮，因为轮次由企业端控制。条件满足时显示"继续下一轮"按钮，点击后 `handleContinue` 函数计算 `nextRound = currentRound + 1`，通过 `navigate` 携带完整 state 跳转回 Practice 页面，state 中包含 `continueRound: true`、`currentRound` 和 `previousFeedback` 等参数。Practice 页面的 `useEffect` 检测到 `continueRound` 后自动调用 `/interview/start` 接口并附加 `isContinuation: true`。续轮时 AI 提示词会注入前一轮的评估反馈，问题数按 `Math.max(3, 5 - currentRound + 1)` 递减，让多轮面试逐步深入而非简单重复。

---

## 第 12 页 — 浏览职位界面展示

这是学生端的职位浏览页面。上方提供公司名称、面试轮次、面试类型和职位状态四个筛选条件，下方以卡片网格展示职位。

技术实现上，筛选参数通过 React Router 的 `useSearchParams` 实现双向同步：初始化时从 URL 读取参数作为默认值，每次筛选变化时通过 `setSearchParams` 更新 URL 查询字符串，刷新页面后筛选条件不丢失。后端 `listJobs` 接口根据参数构建 MongoDB 查询：轮次筛选使用 `$size` 操作符匹配 rounds 数组长度；类型筛选用 `$in` 匹配 rounds.type 字段；公司名称使用 `RegExp` 构建不区分大小写的模糊搜索。最终通过 `populate` 关联 companyId 获取公司名称和 Logo。投递采用乐观更新策略：前端维护一个 `localAppliedIds` 的 Set，投递成功后立即将 jobId 加入集合，无需等待重新请求即可从"立即申请"切换为"已申请"。后端 Application 模型通过 `candidateId + jobId` 的复合唯一索引防止重复投递。

---

## 第 13 页 — 我的申请界面展示

这是学生的申请管理页面，所有投递记录按 5 种状态分组展示（已申请、面试中、已通过、最终通过、已拒绝），每组可折叠展开。

技术实现上，后端 `getMyApplications` 接口通过 `Application.find({ candidateId })` 查询当前学生的所有申请，并连续调用三个 `populate` 分别关联 jobId（职位信息）、candidateId（候选人信息）和 resumeId（简历信息），避免 N+1 查询问题。对于缺失 resumeId 的记录，接口在循环外统一查询一次学生的 resume 对象批量补充，而不是逐条查询。前端拿到数据后按 status 字段的 5 个枚举值分组渲染，每组使用可折叠的 Card 组件展示。

---

## 第 14 页 — 面试状态流转功能展示（一）

两张截图展示了学生端申请详情的两种初始状态：左侧"等待企业审核"，右侧"已被拒绝"。

技术实现上，后端 `updateApplicationStatus` 接口是状态流转的核心。它首先验证 status 值是否在 5 个合法枚举中，然后根据 previousStatus 和目标 status 的组合更新 `approvedThrough` 字段：从 applied 变为 in-progress 时设为 1，从 in-progress 变为 in-progress 时递增，从 in-progress 变为 selected 时也递增。学生端只有当 `currentRound < approvedThrough` 时才渲染"开始面试"按钮，确保只有企业已审批通过的轮次才能启动。rejected 状态可从任意状态进入且操作不可逆。

---

## 第 15 页 — 面试状态流转功能展示（二）

左侧是 DevOps 高级工程师的 3 轮面试进度，当前在第 1 轮技术面。右侧是 1 轮面试通过后的等待审核状态。

技术实现上，前端 `StudentApplicationDetailPage.tsx` 维护一个 `currentStep` 状态机（waiting → interview → results），控制显示详情页、面试界面或结果页。开始面试时，先通过 `GET /resume/:id/text` 获取简历文本并缓存，然后根据 `job.rounds[currentRound]` 取出当前轮次的面试类型和难度，调用 `POST /interview/start` 并传入 `type: "company"` 标识为企业面试。面试结束后调用 `POST /applications/:id/round` 的 `addRoundResult` 函数，将 roundNumber、interviewId、result 和 feedback 通过 `history.push` 写入申请记录。如果 result 为 success 且 roundNumber 达到总轮数，状态自动变为 selected；如果为 failure 则直接变为 rejected。

---

## 第 16 页 — 面试状态流转功能展示（三）

左侧是数据分析师职位的"恭喜！您已最终通过"状态。右侧展示 AI 评估反馈，使用 `MarkdownRenderer.tsx` 渲染。

整个申请状态严格遵循状态机：applied → in-progress → selected → final-selected，rejected 可从任意状态进入。状态机的关键是 `approvedThrough` 字段的协调机制：企业端通过 `updateApplicationStatus` 更新状态时递增该字段，学生端渲染开始面试按钮时比较 `currentRound` 与 `approvedThrough`，只有后者大于前者才允许启动。这种设计解决了企业与学生在不同端异步操作的协调问题——企业审批一轮后学生才能开始该轮面试，避免学生超前完成未授权的轮次。

---

## 第 17 页 — 简历上传与解析进度展示

这是学生个人资料页面，展示基本信息和简历管理区。当前正在进行 OCR 识别，进度条显示 79%。

简历解析采用前后端协作的降级策略。后端 `getResumeTextById` 函数首先检查 Resume 文档的 text 字段是否已缓存，有则直接返回。未缓存时根据 fileType 分支处理：PDF 文件通过 `pdfjs-dist` 的 `getDocument` 加载文档，循环调用 `getPage` 和 `getTextContent` 提取每页文本；DOCX 文件通过 `mammoth` 的 `extractRawText` 提取纯文本。解析成功后将文本写入 Resume.text 字段缓存，避免重复解析。当 PDF 为扫描件无法提取文字时，后端返回 422 错误码，前端 `ResumeUploader.tsx` 捕获后自动启动 OCR 降级：通过 `pdfjs-dist` 将 PDF 页面渲染为 Canvas（scale=2.0 保证清晰度），转为 Base64 图片后调用 `tesseract.js` 进行中英文混合识别（语言参数 `eng+chi_sim`）。OCR 进度通过加权公式实时更新：`30 + (i/totalImages)*70 + (progress*70/totalImages)`，其中前 30% 为 PDF 渲染阶段，后 70% 按页数平分给识别阶段。这种策略将计算密集的 OCR 负载分散到客户端，减轻服务器压力。

---

## 第 18 页 — 企业端功能实现（过渡页）

接下来展示企业端的核心功能。

---

## 第 19 页 — 企业端仪表盘界面展示

这是企业仪表盘页面。顶部 4 个统计卡片显示职位总数、申请总数、已通过和已拒绝数量。核心区域是申请流程可视化：左侧 SVG 环形图按 5 种状态分段着色显示各阶段候选人分布，右侧进度条展示各阶段转化率。

环形图使用纯 SVG 手动绘制，不依赖第三方图表库。核心实现在 `Dashboard.tsx` 中：circle 元素半径 radius=85，计算 `circumference = 2×π×85` 得到周长。每个状态段之间预留 gap=3 像素间隔，可用弧长 = 周长减去总间隙。每段长度按 `(该状态数量/总数)×可用弧长` 比例分配。通过 `strokeDasharray` 设置为"段长度 周长-段长度"控制可见弧段，`strokeDashoffset` 控制起始偏移将弧段从默认的 3 点钟位置旋转到 12 点钟方向逐段排列。动画通过 CSS `transition-all duration-700` 实现入场效果。支持鼠标悬停高亮：非悬停段 opacity 降为 0.4。通过率按 `(selected + finalSelected) / total × 100%` 计算，以渐变文字展示在环形图中央。右侧进度条使用 CSS transition 实现从 0 增长到目标宽度的入场动画（duration=700ms）。数据通过 `useFetch` 调用 `fetchCompanyDashboard` API 获取，后端 `companyController.js` 的 `getCompanyDashboard` 通过 MongoDB 聚合查询统计各项数据。

---

## 第 20 页 — 职位发布界面展示

这是企业的职位发布表单，包含职位名称、描述、技能要求和面试环节配置，每轮可设置类型、难度、主题和备注，最多 3 轮。

技术实现上，`CompanyJobForm.tsx` 通过动态数组管理轮次列表，每轮的数据结构包含 type（面试类型：technical/behavioral/hr）、difficulty（难度：beginner/intermediate/senior）、topic 和 notes。提交时调用 `POST /jobs` 接口，后端 `jobController.js` 的 `createJob` 函数校验 title 和 description 不为空、rounds 为非空数组后，通过 `JobOpening.create` 创建职位文档，companyId 从 `req.user.id` 获取（JWT 中间件注入），默认 status 为 open。轮次配置保存在 JobOpening 文档的 rounds 嵌套数组中，Mongoose Schema 通过 type 和 difficulty 的枚举约束确保数据合法性。

---

## 第 21 页 — 职位列表界面展示

这是企业的职位管理列表，6 个已发布职位以 2 行 3 列卡片网格展示，包含名称、描述、技能标签和招聘状态。

企业可通过卡片右下角的电源按钮图标一键切换职位的开启和关闭状态，调用 `PATCH /jobs/:id/status` 接口，后端通过 `JobOpening.findByIdAndUpdate` 更新 status 字段（open/closed），关闭的职位在学生端的 `listJobs` 查询中会被自动过滤掉。页面左下角有"发布新职位"按钮，通过 React Router 导航到职位创建表单。

---

## 第 22 页 — 发布职位状态追踪界面展示

这是单个职位下的申请管理页面，候选人按五种状态分组展示，每组可折叠展开。候选人卡片显示姓名、邮箱和申请时间，点击跳转到详情页。

技术实现上，后端 `getJobApplications` 接口通过 `Application.find({ jobId })` 查询该职位下所有申请，populate 关联 candidateId 和 resumeId 获取候选人信息和简历。对于缺失 resumeId 的记录，通过 `candidateId.resumeId` 反向查找并补充。前端使用 `useParams` 从 URL 获取 jobId，数据加载后按 status 字段分组渲染，状态颜色和标签从统一的 `constants/status.ts` 常量文件导入，保持前后端一致。

---

## 第 23 页 — 企业端职位状态流转功能展示（一）

这是企业端申请详情页面，标题显示候选人"苏念"申请的"AI应用开发工程师"职位，下方有"批准→进入面试"和"拒绝"操作按钮。

技术实现上，`ApplicationDetailPage.tsx` 根据申请状态动态渲染操作按钮。点击"批准→进入面试"后，调用 `PATCH /applications/:id` 的 `updateApplicationStatus` 接口，将状态从 applied 变为 in-progress，同时设置 `approvedThrough = 1`。此时还会触发邮件通知：后端通过 `Company.findById` 获取企业名称，调用 `emailService.js` 的 `sendInterviewApprovalEmail` 函数，通过 nodemailer + QQ SMTP 异步发送面试批准邮件给候选人。邮件发送不使用 await，而是通过 `.catch` 捕获错误仅记录日志，避免邮件失败影响主流程。点击"拒绝"则直接变为 rejected，操作不可逆。

---

## 第 24 页 — 企业端职位状态流转功能展示（二）

左侧是企业端查看候选人评估结果页面，展示完整的 AI 综合评价。底部操作区域显示"学生已通过所有面试轮次，请进行最终审核"，提供"最终批准"和"拒绝"按钮。

整个流程形成闭环：学生在完成所有轮次面试后，`addRoundResult` 接口检测到最后一轮通过，将 status 设为 selected。企业端详情页检测到 status 为 selected 且 history 中所有轮次均为 success 时，显示最终审批界面。企业点击"最终批准"后调用 `updateApplicationStatus` 将 status 更新为 final-selected，完成整个招聘流程。两端通过 Application 文档共享状态实现协调——企业通过 approvedThrough 控制学生可启动的轮次，学生通过 addRoundResult 推进面试进度并反馈结果。

---

## 第 25 页 — 企业端查看候选人简历界面展示

这是企业点击"查看简历"后弹出的简历预览弹窗，副标题"已提取并自动格式化以便于阅读"。简历内容经过 AI 格式化后结构清晰，展示了教育背景、专业技能和项目经验。

技术实现上，`ResumeViewer.tsx` 组件以全屏模态框展示，采用 SSE 流式调用 `POST /resume/format-resume-stream` 接口。后端 `formatResumeStream` 函数将简历原始文本传入 `resumeFormatPrompt` 提示词生成器，该提示词指示 DeepSeek AI 将非结构化文本整理为包含个人简介、技能、项目经验、教育背景等七个固定板块的 Markdown 格式，并明确要求不添加虚假信息。然后通过 `streamDeepSeekResponse` 异步生成器逐块输出格式化结果，前端通过 `MarkdownText.tsx` 组件（基于 ReactMarkdown + remarkGfm）实时渲染流式返回的内容。

---

## 第 26 页 — 结束页

以上就是 IntelliHire 平台的核心功能演示。本系统采用前后端分离架构，前端基于 React 19 和 TypeScript 构建单页面应用，后端使用 Express 5 提供 RESTful API 服务，数据库选用 MongoDB 配合 Mongoose 实现文档存储，AI 能力依托 DeepSeek 大语言模型驱动面试交互和评估反馈。系统为学生端提供了 AI 模拟面试练习、简历管理、职位浏览和申请管理功能，为企业端提供了职位发布、候选人管理和招聘数据可视化功能。请各位老师批评指正。
