# Repository Guidelines

## 项目结构与模块组织

这是一个前后端分离的 Node.js 项目：`client/` 提供浏览器端界面，`server/` 提供 REST API、鉴权与数据访问。进入其中任一目录工作前，先阅读该目录的 `AGENTS.md`；子目录规则补充本文件，不替代本文件。根目录 `README.md` 是启动、环境变量和 API 概览的权威参考。

## 构建、检查与本地开发

依赖分别安装在两个工作区：`cd client && npm install` 与 `cd server && npm install`。本地联调通常需要同时运行前端与后端开发服务；前端默认请求 `VITE_API_URL` 指向的 API。改动跨越客户端与服务端时，先明确接口契约、错误状态和兼容方案，再分别实施变更。

## 代码风格与命名

沿用临近文件的格式、命名与模块边界；避免无关重排或大范围格式化。共享字段、接口路径和状态常量不能在两端各自复制定义：变更时同步更新客户端类型、调用方、服务端校验与响应。新增核心流程时，在关键分支处添加简短中文注释，说明业务约束而非逐行复述代码。

## 测试与验证

提交前执行受影响工作区可用的检查命令，并手动走通改动涉及的关键路径。新增自动化测试时，测试名称应描述可观察行为，例如 `returns 401 for expired token`。涉及鉴权、上传、流式面试或角色权限的改动，必须覆盖正常与失败场景；若无法执行检查，应在 PR 中说明原因和替代验证方式。

## 架构与接口边界

接口变动采用可渐进迁移的方式：先保证服务端对旧调用的兼容性，再更新客户端，最后移除废弃路径。不要在 UI 中直连数据库、也不要让路由模块承载复杂业务逻辑。失败响应不得暴露堆栈、密钥、内部地址或第三方供应商敏感信息。

## 安全与配置

仅从环境变量读取数据库、JWT、DeepSeek、邮件和 OSS 凭据；禁止提交 `client/.env`、`server/.env` 或真实密钥。前端以 `VITE_` 前缀暴露的变量均可被浏览器读取，不能存放秘密。配置项变化应同步更新 README，并确认本地、测试和生产环境均有安全默认值。

## 提交、PR 与配置

提交历史采用 Conventional Commits，如 `feat: add interview flow`、`fix: handle expired token` 和 `refactor: simplify validation`。标题简短、使用祈使语气且只描述核心改动。PR 应说明用户可见变化、验证命令和关联 issue；界面修改附截图，接口或模型修改说明兼容性、迁移影响与回滚方式。
