# IntelliHire 答辩模拟问答

> 按问题类型分类，涵盖技术实现、架构设计、AI 应用、测试验证和系统优化五大方向，共 25 个高频问题。

---

## 一、技术架构与选型

### Q1：为什么选择前后端分离架构？有没有考虑过其他架构？

**回答：**

选择前后端分离主要基于三个考虑。第一是开发效率，前后端可以并行开发，前端通过 Mock 数据独立推进界面开发，后端专注 API 设计，互不阻塞。第二是职责清晰，前端 React 负责用户交互和页面渲染，后端 Express 专注业务逻辑和数据处理，代码边界明确，便于维护和团队协作。第三是灵活性，前后端独立部署，前端可以部署到 CDN 加速静态资源，后端可以独立水平扩展 API 服务。

也考虑过 SSR 方案比如 Next.js，但由于本系统是强交互的应用，AI 面试对话、实时流式传输等场景都需要客户端 JavaScript 深度参与，SSR 的优势不明显，反而增加了复杂度。所以最终选择了纯前端 SPA 加后端 API 的分离架构。

---

### Q2：为什么选择 MongoDB 而不是 MySQL？在什么场景下 MongoDB 更合适？

**回答：**

选择 MongoDB 主要基于数据结构的灵活性考虑。系统中的核心数据比如面试记录（Interview），包含嵌套的问答历史数组 chatHistory 和分块评估反馈数组 feedbacks，这种层级嵌套结构用 MongoDB 的文档模型可以自然地表达，一条文档就能存储完整的面试信息。如果用 MySQL 的关系型表，需要拆分成面试表、问答表、反馈表多张表，查询时需要多表 JOIN，增加了复杂度。

此外，系统的数据模式在开发过程中有过调整，比如 Application 集合增加了 approvedThrough 字段来追踪多轮面试的轮次控制，MongoDB 无需执行数据库迁移，直接在 Schema 层修改即可。

不过 MongoDB 也有不足，比如不支持多文档事务的强一致性保证（虽然 4.0 后支持了多文档事务，但性能开销较大）。本系统的业务场景以文档级别的操作为主，对跨文档事务的需求不大，所以 MongoDB 是合适的选型。

---

### Q3：为什么使用 JWT 而不是 Session 做认证？JWT 的安全性怎么保证？

**回答：**

选择 JWT 而非 Session 主要因为系统是无状态的 RESTful API 架构。Session 需要在服务端维护状态存储，而 JWT 将认证信息编码在令牌本身中，服务端无需查询数据库即可验证身份，更适合前后端分离的场景。

在安全性方面采取了以下措施：第一，密码使用 bcrypt 加盐哈希存储，saltRounds 设为 10，即使数据库泄露也无法还原明文密码。第二，JWT 令牌设置合理的过期时间——普通登录 1 天、GitHub OAuth 登录 30 天，过期后需重新认证。第三，令牌存储在 localStorage 中，通过 axios 拦截器统一添加到 Authorization 请求头，前端路由守卫在令牌过期时自动跳转登录页。第四，文件上传做了格式和大小校验，API 层面的中间件在每次请求时验证 Token 有效性。

不过 JWT 也有不足，比如无法主动让某个令牌失效（除非引入黑名单机制）。在本系统中这个影响较小，因为令牌有效期较短。

---

## 二、AI 面试核心功能

### Q4：AI 面试官 "艾莎" 是怎么实现的？提示词是怎么设计的？

**回答：**

AI 面试官的实现分为三层。最底层是系统提示词，定义了 "艾莎" 的角色设定，包括她是一位经验丰富的面试官，性格专业但有亲和力。中间层是面试阶段的提示词，分为三个阶段：start 阶段生成开场白和第一个问题，respond 阶段根据学生回答生成追问，conclude 阶段进行评估。

提示词设计中比较关键的点是：在 start 阶段，会将学生的简历摘要、目标职位、难度等级、面试类型等参数注入提示词，让 AI 生成有针对性的问题。在 respond 阶段，会传入历史对话上下文，让 AI 能够基于之前的问答生成连贯的追问。还有一个细节是敷衍检测机制，当 AI 判断学生回答敷衍时，会在回复中嵌入 `[REPROMPT]` 标签，前端检测到这个标签后不推进题号，要求学生重新回答。

---

### Q5：为什么选择 DeepSeek 而不是 ChatGPT 或其他大模型？

**回答：**

选择 DeepSeek 主要基于三个实际考虑。第一是成本因素，DeepSeek API 的定价远低于 OpenAI 的 GPT-4 系列，对于学生毕业设计项目来说，成本可控非常重要。第二是中文能力，DeepSeek 在中文理解和生成方面表现优秀，面试场景大量涉及中文对话，DeepSeek 的中文表现满足需求。第三是 API 接口兼容性，DeepSeek 的 API 接口与 OpenAI 格式兼容，开发时可以使用社区成熟的 SDK，降低了接入成本。

实际使用中 deepseek-chat 模型在面试问答生成、评估反馈撰写等方面表现稳定。当然 DeepSeek 也有不足，比如服务稳定性偶尔波动，对此我实现了指数退避重试机制来应对。

---

### Q6：SSE 流式传输是怎么实现的？和 WebSocket 有什么区别？为什么不选 WebSocket？

**回答：**

SSE 即 Server-Sent Events，是服务端向客户端单向推送数据的技术。实现上，后端设置响应头 `Content-Type: text/event-stream`，然后通过 `res.write()` 逐块推送数据；前端通过 `fetch` 发起请求，使用 `ReadableStream` 的 `reader.read()` 逐块读取，实时拼接渲染 AI 回复。

选择 SSE 而非 WebSocket，是因为本系统是单向推送场景——客户端发送请求后，只需要服务端向客户端推送 AI 回复，不需要双向通信。SSE 基于 HTTP 协议，实现更简单，不需要额外的连接管理逻辑，浏览器原生支持，兼容性好。WebSocket 更适合聊天室、协同编辑等需要双向实时通信的场景。

流式传输的实现细节上，后端监听 `req` 的 `close` 事件，当客户端断开连接时停止推送，避免资源浪费。同时设置了 `X-Accel-Buffering: no` 响应头，防止 Nginx 等反向代理缓冲流式输出。

---

### Q7：分块评估算法的设计思路是什么？为什么不一次性评估？

**回答：**

分块评估是将面试问答按每 3 对一组进行分块，分别调用 AI 生成分块反馈，最后汇总生成综合评价。这个设计基于两个考虑：

第一是评估精度。当问答数量较多（比如 8 个问题就是 16 条记录），一次性提交给 AI 评估会导致上下文过长，AI 的注意力会分散，评估精度下降，可能遗漏前面的问题细节。按 3 对分块后，每块聚焦于局部表现，AI 能更细致地分析每一段回答。

第二是实时体验。分块评估的结果通过 SSE 逐块推送，学生不需要等到所有评估完成才能看到反馈。配合自定义的 SSE 标记协议（`[CHUNK_START]`、`[CHUNK_END]`、`[FINAL_START]`、`[DONE]`），前端用三阶段状态机（idle→chunk→final）准确解析并实时渲染各阶段评估内容。

最终汇总阶段，将所有分块反馈和面试参数一起提交给 AI，从技术深度、表达能力、问题理解、逻辑思维和综合印象五个维度生成评价，并通过正则表达式自动提取 "通过" 或 "不通过" 的结论。

---

### Q8：敷衍检测机制是怎么实现的？误判怎么处理？

**回答：**

敷衍检测依赖 AI 的判断能力。在 AI 面试官的 respond 阶段提示词中，明确要求 AI 判断学生回答是否敷衍，如果判定敷衍则在回复中嵌入 `[REPROMPT]` 标签。

前端收到 AI 回复后，通过 `isPerfunctoryReprompt` 函数检测是否包含该标签。如果检测到，执行三个操作：第一，用 `stripRepromptTag` 移除标签保持显示干净；第二，题号不递增，学生需要重新回答当前问题；第三，AI 的追问内容会引导学生给出更有深度的回答。

关于误判，测试用例专门验证了这一点。比如学生回答 "这个领域我不太熟悉，但根据我的理解……"，虽然表示不熟悉但后续给出了力所能及的分析，这种诚实回答不会被判定为敷衍。AI 会基于回答的内容充实度、与问题的相关性等综合判断，不是简单的字数判断。

---

## 三、系统功能实现细节

### Q9：多轮面试是怎么实现的？轮次之间怎么衔接？

**回答：**

多轮面试的实现涉及前端状态管理和后端上下文传递两个方面。

前端层面，学生在面试结果页面点击 "继续下一轮" 后，通过 `location.state` 传递轮次上下文（当前轮次、总轮数、上一轮反馈）给面试配置组件。为了解决页面刷新导致状态丢失的问题，使用了 localStorage 存储 `pendingNextRound` 信息，包含面试 ID 和下一轮轮次编号，页面加载时优先从 localStorage 恢复续轮状态。

后端层面，续轮面试的 start 接口接收 `isContinuation=true` 和 `previousFeedback` 参数，在构造 AI 提示词时额外包含前一轮的评估反馈，让 AI 知道学生在哪些方面表现好、哪些方面需要改进，从而调整后续问题的难度和重点方向。每轮面试生成独立的 Interview 记录，通过 Application 的 history 数组关联。

---

### Q10：申请状态流转的设计是怎样的？approvedThrough 字段的作用是什么？

**回答：**

申请状态定义了 5 个状态值：applied（已申请）→ in-progress（面试中）→ selected（已通过）→ final-selected（最终通过），以及 rejected（已拒绝，可从任意状态进入）。

approvedThrough 字段是多轮面试轮次控制的关键。它表示企业已批准开启到第几轮面试。当企业 "批准进入面试" 时，approvedThrough 设为 1；当企业 "开启下一轮" 时，approvedThrough 递增。学生端只有当 `currentRound < approvedThrough` 时，才能启动对应轮次的企业面试。这种设计实现了企业和学生之间的异步协调——企业审批后学生才能操作，避免了轮次混乱。

具体流转示例：对于 3 轮面试的职位，学生投递后状态为 applied、approvedThrough = 0；企业批准后变为 in-progress、approvedThrough = 1，学生可以开始第 1 轮；第 1 轮完成后，企业点击 "开启下一轮"，approvedThrough = 2，学生可以开始第 2 轮；全部轮次通过后，状态变为 selected，企业执行 "最终批准" 变为 final-selected。

---

### Q11：简历解析为什么采用前后端分离的策略？OCR 放在前端有什么考虑？

**回答：**

简历解析采用前后端协作的降级策略：后端负责 PDF（pdfjs-dist）和 DOCX（mammoth）的文本提取，这是主要路径；当后端无法提取文字（比如扫描件 PDF 返回 422 错误）时，前端自动启动 OCR 降级。

OCR 放在前端有三个考虑。第一是计算负载，OCR 是 CPU 密集型操作，扫描件 PDF 每页的识别通常需要 3-5 秒，如果放在后端，多用户同时上传会占用大量服务器资源。前端利用客户端的空闲算力，分散了计算压力。第二是隐私保护，简历图片不需要上传到第三方 OCR 服务，tesseract.js 完全在浏览器本地运行。第三是实时反馈，前端可以精确追踪 tesseract.js 的识别进度，展示进度条和预估剩余时间，用户体验更好。

进度计算使用了加权公式：前 30%预分配给 PDF 转图片阶段，后 70%按 OCR 页面数和 tesseract 内部进度动态计算，确保进度条平稳推进。

---

### Q12：GitHub OAuth 的账户合并是怎么实现的？

**回答：**

GitHub OAuth 登录流程是：用户先选择角色，然后跳转 GitHub 授权页面，授权后 GitHub 回调到后端接口，后端用授权码换取 access_token，再通过 access_token 获取 GitHub 用户信息。

账户合并的逻辑是：如果 GitHub 账号中的邮箱已经在系统中注册过（无论是邮箱密码注册的还是之前关联的），就直接将 githubId 写入该用户记录，实现账户合并。用户后续既可以用邮箱密码登录，也可以用 GitHub 登录，都会进入同一个账户。如果 GitHub 邮箱在系统中不存在，则创建新用户并关联 githubId。

JWT 令牌方面，GitHub 登录的令牌有效期设为 30 天，比普通登录的 1 天更长，减少第三方登录用户的重复授权频率，提升体验。

---

## 四、性能与可靠性

### Q13：指数退避重试机制的设计原理是什么？为什么 4xx 不重试？

**回答：**

指数退避重试的核心思想是：遇到暂时性错误时，不要立即重试，而是按指数增长的间隔（1 秒、2 秒、4 秒）逐步重试，给服务端恢复的时间，避免密集重试加剧问题。

默认最多重试 2 次，即最多执行 3 次请求。重试次数和超时时间都可以通过环境变量配置。

4xx 错误不重试是因为：4xx 表示客户端错误，比如 400 是请求参数错误、401 是认证失败、422 是数据验证失败，这些错误的根本原因是请求本身有问题，重试多少次结果都一样，反而浪费时间。而 5xx 是服务端临时故障（比如 502 网关错误、503 服务不可用），网络超时可能是瞬时的网络波动，这些情况下重试大概率能成功。

实际运行中，多数 5xx 错误在第一次重试（间隔 1 秒）时就能恢复，效果明显。

---

### Q14：系统目前只做了黑盒功能测试，有没有考虑过性能测试和压力测试？

**回答：**

目前确实只完成了黑盒功能测试，共设计了 70 个测试用例，覆盖 8 个功能模块，全部通过。这是因为毕业设计的时间和精力有限，优先保证了功能完整性和正确性。

如果要做性能测试，我会关注以下几个场景：第一是 SSE 流式传输的并发能力，多个学生同时进行面试时，服务端能否稳定推送流式数据；第二是 DeepSeek API 的调用延迟和吞吐量，这是系统的外部依赖瓶颈；第三是 MongoDB 在高并发读写下的响应时间，尤其是面试记录这种嵌套较深的文档。

可能的优化方向包括：对 API 接口引入缓存（比如职位列表可以缓存），对 SSE 连接数做限制和队列管理，以及考虑将 AI 评估的并行调用改为队列顺序执行，避免同时占用过多 API 配额。

---

### Q15：SSE 流式传输中，如果 AI 回复中间断开了怎么办？

**回答：**

SSE 流式传输的断连处理分两个层面。后端层面，监听 `req` 的 `close` 事件，当检测到客户端断开连接时，停止向 DeepSeek API 读取数据并停止向客户端推送，避免资源浪费。前端层面，如果读取流的过程中发生异常，`reader.read()` 会抛出错误，被外层的 try-catch 捕获，显示友好的错误提示，学生可以重新提交回答。

对于面试对话阶段的影响较小，因为问答记录是实时保存到数据库的——每次学生回答后，后端会将问答对追加到 Interview 的 chatHistory 中。所以即使连接断开，之前的内容不会丢失，学生刷新页面后可以继续面试。

评估阶段的断连风险更大，因为评估是面试结束后统一进行的。不过评估可以重新触发，不影响面试本身的完整性。

---

## 五、数据安全与隐私

### Q16：用户密码是怎么存储的？安全性如何？

**回答：**

密码使用 bcrypt 库进行加盐哈希存储，saltRounds 设为 10。bcrypt 的优势在于它是慢哈希算法，每次哈希计算需要约 100 毫秒，这意味着暴力破解的成本很高——即使攻击者获取了数据库中的哈希值，尝试破解一个 8 位混合密码也需要数年时间。而 MD5、SHA-256 等快速哈希算法可以用 GPU 每秒尝试数十亿次，安全性远不如 bcrypt。

此外，bcrypt 自动为每个密码生成独立的盐值（salt），即使两个用户使用相同密码，存储的哈希值也完全不同，防止了彩虹表攻击。

认证流程是：用户登录时提交明文密码，后端使用 `bcrypt.compare()` 将明文与存储的哈希值比对，验证通过后签发 JWT 令牌。明文密码不会存储在数据库中，也不会写入日志。

---

### Q17：JWT 令牌过期后是怎么处理的？

**回答：**

JWT 令牌过期后，前端有两种处理机制。第一是主动检测，axios 响应拦截器捕获 401 状态码，判断错误信息中是否包含 "expired" 关键词，如果是则清除 localStorage 中的 token，弹出提示 "登录已过期"，然后跳转到登录页面。第二是路由守卫，导航组件在每次路由变化时检查 localStorage 中的 token 是否存在，如果不存在则重定向到登录页。

普通登录的 JWT 有效期是 1 天，GitHub OAuth 登录是 30 天。这个设计是权衡了安全性和用户体验——频繁重新登录会降低体验，但令牌有效期过长又有安全风险。

当前没有实现令牌刷新机制（Refresh Token），这是一个可以改进的方向。引入 Refresh Token 后可以在 Access Token 过期时自动续期，无需用户重新登录。

---

## 六、论文相关

### Q18：你的论文第六章写了四个典型问题，请简要说明解决了哪些技术难点

**回答：**

第六章记录了开发过程中遇到的四个关键问题及解决方案。

第一个是 AI 流式响应与评估结果实时解析。核心难点是在同一 SSE 连接中混合传输分块评估和最终评估，前端需要准确区分不同阶段。解决方案是设计了自定义 SSE 标记协议，用 `[CHUNK_START]`、`[CHUNK_END]`、`[FINAL_START]`、`[DONE]` 标记分隔不同阶段，前端用三阶段状态机（idle→chunk→final）逐行解析。

第二个是 DeepSeek API 调用不稳定。5xx 错误导致面试中断。解决方案是实现了指数退避重试机制，4xx 不重试、5xx 按指数间隔重试，默认最多 2 次。

第三个是扫描件 PDF 简历文字提取失败。后端 pdfjs-dist 无法提取扫描件中的文字。解决方案是前端 OCR 降级，使用 tesseract.js 在浏览器本地进行图片 OCR 识别，支持中英混合。

第四个是多轮面试状态丢失。页面刷新后续轮状态消失。解决方案是使用 localStorage 持久化 `pendingNextRound` 信息，页面加载时自动恢复。

---

### Q19：系统有哪些不足？如果继续开发，你会怎么改进？

**回答：**

系统目前在以下几个方面存在不足：

第一，缺少性能测试和压力测试。目前只做了 70 个黑盒功能测试用例，没有对 SSE 并发连接数、API 响应时间、数据库查询性能等做量化评估。后续可以引入 JMeter 或 k6 进行负载测试。

第二，AI 评估的公平性和可解释性有待提升。当前的评估完全依赖 DeepSeek 大模型的判断，评估维度和权重不够透明。未来可以引入结构化评分量表，对每个维度设定明确的评分标准，并让 AI 按标准逐项打分，提升评估的客观性。

第三，缺少自动化测试和 CI/CD 流程。目前测试都是手动执行，后续可以引入 Jest 单元测试和 Playwright 端到端测试，配合 GitHub Actions 实现持续集成。

第四，没有实现消息推送机制。企业审批后的通知目前依赖邮件，学生需要手动刷新页面查看。后续可以引入 WebSocket 实现实时通知。

第五，简历解析对非中英文内容的支持不够。OCR 目前只配置了 `eng+chi_sim`，对于包含日语、韩语等其他语言的简历识别效果不佳。

---

## 七、综合理解

### Q20：你的系统和市面上的招聘平台（如 BOSS 直聘）有什么区别？

**回答：**

核心区别在于定位不同。BOSS 直聘等平台是信息撮合平台，重点在简历投递和在线沟通，解决的是 "招聘信息匹配" 的问题。而 IntelliHire 的定位是 AI 面试练习与招聘的双端服务平台，有三个差异化特点：

第一，面向学生端提供 AI 模拟面试练习功能。学生可以自定义面试参数进行无限次练习，获得详细的评估反馈和改进建议，这是传统招聘平台不具备的。BOSS 直聘等平台虽然也在引入 AI 功能，但主要聚焦在简历优化、智能推荐等辅助环节。

第二，AI 评估贯穿整个面试流程。从模拟练习到企业面试，AI 都参与评估并提供反馈，形成了一个完整的闭环——学生练习 → 获得反馈 → 改进 → 投递 → 企业面试 →AI 再次评估。

第三，多轮面试的精细化控制。通过 approvedThrough 字段实现企业和学生之间的轮次协调，企业可以逐步开启面试轮次，配合 AI 评估结果做决策。

---

### Q21：AI 评估的准确性怎么保证？如果 AI 给了错误评价怎么办？

**回答：**

AI 评估的准确性保障主要从三个方面入手：

第一是提示词工程。评估提示词要求 AI 从明确的维度进行评价，包括技术深度、表达能力、问题理解和逻辑思维，每个维度都有具体的评价标准，限制了 AI 自由发挥的空间。

第二是分块策略。每 3 对问答单独评估，AI 的注意力集中在局部内容上，评估更细致。最终汇总时综合所有分块反馈，兼顾细节和全局。

第三是敷衍检测机制。AI 会判断学生是否认真回答，如果检测到敷衍则要求重新回答，避免了学生用低质量回答 "骗" 过评估的情况。

当然，AI 评估不可能 100%准确，这是大语言模型的固有限制。目前的策略是将 AI 评估作为参考而非绝对标准——学生端的模拟面试评估主要用于自我提升，企业端也只是辅助工具，最终决策仍由企业做出。未来可以引入多模型交叉评估，让不同 AI 独立评估后取综合结果，提升客观性。

---

### Q22：为什么面试配置限制了最多 3 轮、每轮最多 8 个问题？

**回答：**

这些限制是基于实际使用场景和 AI 上下文窗口的考量。

3 轮面试的上限对应了真实招聘中最常见的面试流程——技术初面、技术复面、HR 终面。再多轮次（如 4-5 轮）在实际招聘中非常少见，而且会显著增加系统复杂度和 AI 评估的难度。

每轮 8 个问题的上限主要受 DeepSeek API 的上下文窗口限制。每次调用 API 时会传入面试上下文（提示词+简历摘要+历史对话），如果问题数过多，上下文会超出模型的有效处理范围，导致 AI 回复质量下降。8 个问题加上对话历史大约在 4000-6000 token 左右，处于模型的最佳工作区间。

当然这些参数都是可配置的，如果模型能力提升或使用更大上下文的模型，可以轻松调整上限。

---

### Q23：前后端是怎么协作的？接口规范是怎么定义的？

**回答：**

前后端通过 RESTful API 协作，遵循统一的接口规范。API 路由按功能模块划分前缀：`/api/auth` 负责认证、`/api/interview` 负责面试、`/api/jobs` 负责职位、`/api/applications` 负责申请、`/api/resume` 负责简历、`/api/company` 负责企业管理。

接口的请求和响应格式统一：成功的响应返回 JSON 数据，失败的响应包含明确的错误码和错误信息。文件上传使用 multipart/form-data 格式。

SSE 流式接口比较特殊，比如面试对话和面试评估接口，响应头设为 `text/event-stream`，不走 JSON 格式，而是通过自定义标记协议传输结构化数据。

前端通过 axios 实例统一管理，baseURL 配置为环境变量 `VITE_API_URL`，请求拦截器自动添加 JWT Bearer Token 到 Authorization 头，响应拦截器统一处理 401 认证过期。

---

### Q24：导航栏根据角色动态渲染的实现原理是什么？

**回答：**

Navigation 组件内部维护了三套菜单配置：未登录用户看到首页、登录、注册；学生用户看到仪表盘、模拟面试、浏览职位、我的申请、个人资料；企业用户看到仪表盘、发布职位、职位列表、公司资料。

角色判断的逻辑是：组件从 localStorage 读取 token 和 role 字段，如果 token 存在，根据 role 值选择对应菜单配置；如果 token 不存在，显示公开菜单。

为了保持菜单状态与路由同步，组件使用了 useEffect 监听路由变化（通过 React Router 的 useLocation），每次路由变化时重新读取 localStorage 中的 token 和 role，确保登出后菜单立即切换为公开菜单，登录后显示对应角色的菜单。主题切换按钮和退出登录确认弹窗也是导航栏的组成部分。

---

### Q25：企业仪表盘的 SVG 环形图是怎么实现的？

**回答：**

环形图使用纯 SVG 的 `<circle>` 元素绘制，核心是利用 `stroke-dasharray` 和 `stroke-dashoffset` 两个属性控制弧段的可见长度和起始位置。

实现步骤是：首先计算圆环周长 `circumference = 2 × π × radius`，radius 设为 85。然后为每个分段预留 3 像素间隔，计算可用于绘制弧段的总长度。接着按各阶段申请数量占比分配弧段长度。最后设置每个 circle 的 `stroke-dasharray` 控制可见弧长，`strokeDashoffset` 控制起始偏移，从 12 点钟方向开始顺时针排列。

交互效果方面，通过 React 的 state 追踪当前悬停的分段索引，非悬停分段的 opacity 降为 0.4，实现高亮聚焦效果。通过率通过公式 `(selected + finalSelected) / total × 100%` 计算，以渐变文字样式显示在环形图中央。

整个组件是纯前端实现，不依赖任何图表库，代码可控性高，便于定制样式和交互。
